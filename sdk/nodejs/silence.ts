// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manages a groundcover Silence.
 *
 * Silences allow you to suppress alerts for a specific time window based on matching criteria. This is useful for planned maintenance, deployments, or other situations where you want to temporarily mute alerts.
 *
 * A silence is defined by: a time window (starts_at, ends_at), a comment describing the reason for the silence, and one or more matchers that define which alerts to silence.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as groundcover from "@groundcover-com/groundcover";
 *
 * const config = new pulumi.Config();
 * const groundcoverApiKey = config.require("groundcoverApiKey");
 * const groundcoverBackendId = config.require("groundcoverBackendId");
 * // Example 1: Simple silence for planned maintenance
 * // Silences all alerts for a specific service during a maintenance window
 * const maintenanceWindow = new groundcover.Silence("maintenanceWindow", {
 *     startsAt: "2030-01-15T10:00:00Z",
 *     endsAt: "2030-01-15T14:00:00Z",
 *     comment: "Planned maintenance window for payment-service",
 *     matchers: [{
 *         name: "service",
 *         value: "payment-service",
 *         is_equal: true,
 *         is_contains: false,
 *     }],
 * });
 * // Example 2: Silence with multiple matchers
 * // Silences alerts for a specific workload in a specific environment
 * const deploymentSilence = new groundcover.Silence("deploymentSilence", {
 *     startsAt: "2030-01-20T08:00:00Z",
 *     endsAt: "2030-01-20T10:00:00Z",
 *     comment: "Deploying new version of api-gateway in staging",
 *     matchers: [
 *         {
 *             name: "workload",
 *             value: "api-gateway",
 *             is_equal: true,
 *             is_contains: false,
 *         },
 *         {
 *             name: "environment",
 *             value: "staging",
 *             is_equal: true,
 *             is_contains: false,
 *         },
 *     ],
 * });
 * // Example 3: Silence with contains pattern (starts_at and ends_at are UTC 0)
 * // Silences alerts for any service containing the specified value
 * const testServicesSilence = new groundcover.Silence("testServicesSilence", {
 *     startsAt: "2030-02-01T00:00:00Z",
 *     endsAt: "2030-02-01T06:00:00Z",
 *     comment: "Overnight testing - silence all test services",
 *     matchers: [{
 *         name: "service",
 *         value: "app-dev",
 *         is_equal: true,
 *         is_contains: true,
 *     }],
 * });
 * // Example 4: Silence with negation (is_equal = false)
 * // Silences all alerts EXCEPT for production environment
 * const nonProductionSilence = new groundcover.Silence("nonProductionSilence", {
 *     startsAt: "2030-03-01T12:00:00Z",
 *     endsAt: "2030-03-01T18:00:00Z",
 *     comment: "Silence non-production alerts during load testing",
 *     matchers: [{
 *         name: "environment",
 *         value: "production",
 *         is_equal: false,
 *         is_contains: false,
 *     }],
 * });
 * export const maintenanceSilenceId = maintenanceWindow.id;
 * export const deploymentSilenceId = deploymentSilence.id;
 * ```
 */
export class Silence extends pulumi.CustomResource {
    /**
     * Get an existing Silence resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SilenceState, opts?: pulumi.CustomResourceOptions): Silence {
        return new Silence(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'groundcover:index/silence:Silence';

    /**
     * Returns true if the given object is an instance of Silence.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Silence {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Silence.__pulumiType;
    }

    /**
     * A comment describing the reason for the silence.
     */
    declare public readonly comment: pulumi.Output<string>;
    /**
     * The end time of the silence in RFC3339 format UTC 0 (e.g., `2024-01-15T12:00:00Z`).
     */
    declare public readonly endsAt: pulumi.Output<string>;
    /**
     * A list of matchers that define which alerts to silence. Each matcher specifies a label name and value to match against.
     */
    declare public readonly matchers: pulumi.Output<outputs.SilenceMatcher[]>;
    /**
     * The start time of the silence in RFC3339 format UTC 0 (e.g., `2024-01-15T10:00:00Z`).
     */
    declare public readonly startsAt: pulumi.Output<string>;

    /**
     * Create a Silence resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SilenceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SilenceArgs | SilenceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SilenceState | undefined;
            resourceInputs["comment"] = state?.comment;
            resourceInputs["endsAt"] = state?.endsAt;
            resourceInputs["matchers"] = state?.matchers;
            resourceInputs["startsAt"] = state?.startsAt;
        } else {
            const args = argsOrState as SilenceArgs | undefined;
            if (args?.endsAt === undefined && !opts.urn) {
                throw new Error("Missing required property 'endsAt'");
            }
            if (args?.matchers === undefined && !opts.urn) {
                throw new Error("Missing required property 'matchers'");
            }
            if (args?.startsAt === undefined && !opts.urn) {
                throw new Error("Missing required property 'startsAt'");
            }
            resourceInputs["comment"] = args?.comment;
            resourceInputs["endsAt"] = args?.endsAt;
            resourceInputs["matchers"] = args?.matchers;
            resourceInputs["startsAt"] = args?.startsAt;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Silence.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Silence resources.
 */
export interface SilenceState {
    /**
     * A comment describing the reason for the silence.
     */
    comment?: pulumi.Input<string>;
    /**
     * The end time of the silence in RFC3339 format UTC 0 (e.g., `2024-01-15T12:00:00Z`).
     */
    endsAt?: pulumi.Input<string>;
    /**
     * A list of matchers that define which alerts to silence. Each matcher specifies a label name and value to match against.
     */
    matchers?: pulumi.Input<pulumi.Input<inputs.SilenceMatcher>[]>;
    /**
     * The start time of the silence in RFC3339 format UTC 0 (e.g., `2024-01-15T10:00:00Z`).
     */
    startsAt?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a Silence resource.
 */
export interface SilenceArgs {
    /**
     * A comment describing the reason for the silence.
     */
    comment?: pulumi.Input<string>;
    /**
     * The end time of the silence in RFC3339 format UTC 0 (e.g., `2024-01-15T12:00:00Z`).
     */
    endsAt: pulumi.Input<string>;
    /**
     * A list of matchers that define which alerts to silence. Each matcher specifies a label name and value to match against.
     */
    matchers: pulumi.Input<pulumi.Input<inputs.SilenceMatcher>[]>;
    /**
     * The start time of the silence in RFC3339 format UTC 0 (e.g., `2024-01-15T10:00:00Z`).
     */
    startsAt: pulumi.Input<string>;
}
