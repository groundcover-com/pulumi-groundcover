// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Metrics Pipeline resource. Singleton resource that configures metrics relabeling rules applied before aggregation.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as groundcover from "@groundcover-com/groundcover";
 *
 * const example = new groundcover.Metricspipeline("example", {rules: {
 *     add_label: {
 *         env: "production",
 *         team: "platform",
 *     },
 *     drop_regex: ["go_.*"],
 *     raw: `  - action: labelmap
 *     replacement: "groundcover_1"
 *
 * `,
 * }});
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import groundcover:index/metricspipeline:Metricspipeline example any
 * ```
 */
export class Metricspipeline extends pulumi.CustomResource {
    /**
     * Get an existing Metricspipeline resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: MetricspipelineState, opts?: pulumi.CustomResourceOptions): Metricspipeline {
        return new Metricspipeline(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'groundcover:index/metricspipeline:Metricspipeline';

    /**
     * Returns true if the given object is an instance of Metricspipeline.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Metricspipeline {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Metricspipeline.__pulumiType;
    }

    /**
     * Relabeling rules to apply to metrics.
     */
    declare public readonly rules: pulumi.Output<outputs.MetricspipelineRules | undefined>;
    /**
     * The last update timestamp of the metrics pipeline configuration.
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<string>;

    /**
     * Create a Metricspipeline resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: MetricspipelineArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: MetricspipelineArgs | MetricspipelineState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as MetricspipelineState | undefined;
            resourceInputs["rules"] = state?.rules;
            resourceInputs["updatedAt"] = state?.updatedAt;
        } else {
            const args = argsOrState as MetricspipelineArgs | undefined;
            resourceInputs["rules"] = args?.rules;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Metricspipeline.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Metricspipeline resources.
 */
export interface MetricspipelineState {
    /**
     * Relabeling rules to apply to metrics.
     */
    rules?: pulumi.Input<inputs.MetricspipelineRules>;
    /**
     * The last update timestamp of the metrics pipeline configuration.
     */
    updatedAt?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a Metricspipeline resource.
 */
export interface MetricspipelineArgs {
    /**
     * Relabeling rules to apply to metrics.
     */
    rules?: pulumi.Input<inputs.MetricspipelineRules>;
}
