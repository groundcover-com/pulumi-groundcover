// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manages a groundcover Synthetic Test. Synthetic tests allow you to proactively monitor your services by running periodic HTTP, SSL/TLS, TCP, or DNS checks against specified endpoints.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as groundcover from "@groundcover-com/groundcover";
 *
 * // Example: Simple HTTP health check
 * const httpHealthCheck = new groundcover.SyntheticTest("httpHealthCheck", {
 *     interval: "1m",
 *     httpCheck: [{
 *         url: "https://httpbin.org/status/200",
 *         method: "GET",
 *         timeout: "10s",
 *     }],
 *     assertions: [
 *         {
 *             source: "statusCode",
 *             operator: "eq",
 *             target: "200",
 *         },
 *         {
 *             source: "responseTime",
 *             operator: "lt",
 *             target: "5000",
 *         },
 *     ],
 *     retry: [{
 *         count: 2,
 *         interval: "1s",
 *     }],
 * });
 * // Example: HTTP check with follow_redirects and allow_insecure
 * const httpInsecureCheck = new groundcover.SyntheticTest("httpInsecureCheck", {
 *     interval: "5m",
 *     httpCheck: [{
 *         url: "https://httpbin.org/redirect/1",
 *         method: "GET",
 *         timeout: "10s",
 *         followRedirects: true,
 *         allowInsecure: true,
 *     }],
 *     assertions: [{
 *         source: "statusCode",
 *         operator: "eq",
 *         target: "200",
 *     }],
 * });
 * // Example: HTTP POST with body and headers
 * const httpPostCheck = new groundcover.SyntheticTest("httpPostCheck", {
 *     interval: "5m",
 *     httpCheck: [{
 *         url: "https://httpbin.org/post",
 *         method: "POST",
 *         timeout: "10s",
 *         headers: {
 *             "Content-Type": "application/json",
 *         },
 *         body: [{
 *             type: "json",
 *             content: "{\"test\": \"data\"}",
 *         }],
 *     }],
 *     assertions: [{
 *         source: "statusCode",
 *         operator: "eq",
 *         target: "200",
 *     }],
 * });
 * // Example: Authenticated HTTP check using secrets
 * const authenticatedCheck = new groundcover.SyntheticTest("authenticatedCheck", {
 *     interval: "5m",
 *     httpCheck: [{
 *         url: "https://api.example.com/health",
 *         method: "GET",
 *         timeout: "10s",
 *         auth: [{
 *             type: "bearer",
 *             token: "secretRef::store::your-secret-id",
 *         }],
 *     }],
 *     assertions: [{
 *         source: "statusCode",
 *         operator: "eq",
 *         target: "200",
 *     }],
 * });
 * // Example: With custom labels and severity
 * const performanceCheck = new groundcover.SyntheticTest("performanceCheck", {
 *     interval: "30s",
 *     labels: {
 *         env: "production",
 *         team: "platform",
 *     },
 *     httpCheck: [{
 *         url: "https://api.example.com/status",
 *         method: "GET",
 *         timeout: "5s",
 *     }],
 *     assertions: [
 *         {
 *             source: "statusCode",
 *             operator: "eq",
 *             target: "200",
 *             severity: "critical",
 *         },
 *         {
 *             source: "responseTime",
 *             operator: "lt",
 *             target: "300",
 *             severity: "degraded",
 *         },
 *     ],
 *     retry: [{
 *         count: 3,
 *         interval: "500ms",
 *     }],
 * });
 * // Example: With monitor and notification routing
 * const monitoredCheck = new groundcover.SyntheticTest("monitoredCheck", {
 *     interval: "1m",
 *     httpCheck: [{
 *         url: "https://api.example.com/health",
 *         method: "GET",
 *         timeout: "10s",
 *     }],
 *     assertions: [{
 *         source: "statusCode",
 *         operator: "eq",
 *         target: "200",
 *     }],
 *     monitor: [{
 *         monitorName: "API Health Monitor",
 *         severity: "S1",
 *         issueSummary: "Synthetic check failed: {{ name }}",
 *         issueDescription: "The synthetic test {{ name }} is failing. Check the endpoint health.",
 *         noDataState: "Alerting",
 *         executionErrorState: "Alerting",
 *         renotificationInterval: "1h",
 *         evaluationInterval: [{
 *             interval: "1m",
 *             pendingFor: "0s",
 *         }],
 *         enabledWorkflows: [
 *             "workflow-id-1",
 *             "workflow-id-2",
 *         ],
 *     }],
 * });
 * // Example: With connected apps notification routing
 * const connectedAppsCheck = new groundcover.SyntheticTest("connectedAppsCheck", {
 *     interval: "1m",
 *     httpCheck: [{
 *         url: "https://api.example.com/health",
 *         method: "GET",
 *         timeout: "10s",
 *     }],
 *     assertions: [{
 *         source: "statusCode",
 *         operator: "eq",
 *         target: "200",
 *     }],
 *     monitor: [{
 *         severity: "S2",
 *         notificationMethod: "connectedApps",
 *         connectedApps: [
 *             "slack-app-id",
 *             "pagerduty-app-id",
 *         ],
 *         statusFilters: [
 *             "Alerting",
 *             "Resolved",
 *         ],
 *         disableRenotification: true,
 *     }],
 * });
 * // Example: SSL certificate check with property assertions
 * const sslCheck = new groundcover.SyntheticTest("sslCheck", {
 *     interval: "5m",
 *     sslCheck: [{
 *         host: "example.com",
 *         port: 443,
 *     }],
 *     assertions: [
 *         {
 *             source: "ssl",
 *             property: "certificateValid",
 *             operator: "eq",
 *             target: "true",
 *         },
 *         {
 *             source: "ssl",
 *             property: "certificateExpiresIn",
 *             operator: "gt",
 *             target: "30",
 *         },
 *     ],
 * });
 * // Example: SSL check with TLS version requirement
 * const sslTlsCheck = new groundcover.SyntheticTest("sslTlsCheck", {
 *     interval: "10m",
 *     sslCheck: [{
 *         host: "api.example.com",
 *         port: 443,
 *         verify: true,
 *         minVersion: "1.2",
 *         sni: "api.example.com",
 *         timeout: "10s",
 *     }],
 *     assertions: [
 *         {
 *             source: "ssl",
 *             property: "certificateValid",
 *             operator: "eq",
 *             target: "true",
 *         },
 *         {
 *             source: "ssl",
 *             property: "chainValid",
 *             operator: "eq",
 *             target: "true",
 *         },
 *     ],
 * });
 * // Example: Basic TCP port check
 * const tcpCheck = new groundcover.SyntheticTest("tcpCheck", {
 *     interval: "1m",
 *     tcpCheck: [{
 *         host: "example.com",
 *         port: 5432,
 *     }],
 *     assertions: [{
 *         source: "tcp",
 *         operator: "exists",
 *         target: "true",
 *     }],
 * });
 * // Example: TCP check with send/receive
 * const tcpSendCheck = new groundcover.SyntheticTest("tcpSendCheck", {
 *     interval: "5m",
 *     tcpCheck: [{
 *         host: "example.com",
 *         port: 6379,
 *         send: "PING",
 *         expectResponse: true,
 *         receiveMaxBytes: 1024,
 *         timeout: "10s",
 *     }],
 *     assertions: [{
 *         source: "tcp",
 *         operator: "exists",
 *         target: "true",
 *     }],
 * });
 * export const httpHealthCheckId = httpHealthCheck.id;
 * export const httpPostCheckId = httpPostCheck.id;
 * export const monitoredCheckId = monitoredCheck.id;
 * export const sslCheckId = sslCheck.id;
 * // Example: Basic DNS resolution check
 * const dnsCheck = new groundcover.SyntheticTest("dnsCheck", {
 *     interval: "1m",
 *     dnsCheck: [{
 *         domain: "example.com",
 *         recordType: "A",
 *     }],
 *     assertions: [{
 *         source: "dns",
 *         operator: "exists",
 *         target: "true",
 *     }],
 * });
 * // Example: DNS check with custom resolver and DNSSEC
 * const dnsFullCheck = new groundcover.SyntheticTest("dnsFullCheck", {
 *     interval: "5m",
 *     dnsCheck: [{
 *         domain: "example.com",
 *         recordType: "A",
 *         port: 53,
 *         resolver: "8.8.8.8",
 *         dnssec: true,
 *         timeout: "10s",
 *     }],
 *     assertions: [{
 *         source: "dns",
 *         operator: "exists",
 *         target: "true",
 *     }],
 * });
 * export const tcpCheckId = tcpCheck.id;
 * export const dnsCheckId = dnsCheck.id;
 * ```
 *
 * ## Assertion Property Reference
 *
 * The `property` field in an assertion specifies which property to evaluate within the assertion source. Its usage varies by source:
 *
 * ### `responseHeader`
 *
 * Set `property` to the header name (case-insensitive).
 *
 * Supported operators: `exists`, `notExists`, `eq`, `ne`, `contains`.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * ```
 *
 * ### `jsonBody`
 *
 * Set `property` to a dot-notation path into the JSON response body (e.g. `user.name`, `data.items`).
 *
 * Supported operators: `exists`, `notExists`, `eq`, `ne`, `contains`.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * ```
 *
 * ### `responseBody`
 *
 * Asserts against the entire raw response body as plain text. The `property` field is not used for this source and should be omitted.
 *
 * Supported operators: `eq`, `ne`, `contains`, `exists`, `notExists`.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * ```
 *
 * ### `ssl`
 *
 * Set `property` to one of the following:
 *
 * | Property | Description | Operators | Target example |
 * |---|---|---|---|
 * | `certificateValid` | Whether the certificate is valid | `eq` | `true` / `false` |
 * | `certificateExpiresIn` | Days until certificate expiry | `gt`, `lt` | `30` |
 * | `tlsVersion` | Negotiated TLS version | `eq`, `gt`, `lt` | `1.2` |
 * | `chainValid` | Whether the certificate chain is valid | `eq` | `true` / `false` |
 * | `cipherSuite` | Negotiated cipher suite | `eq`, `contains` | `AES256` |
 *
 * ### `tcp`
 *
 * The `property` field is optional for TCP assertions. When omitted, operators act as connection checks by default.
 *
 * | Property | Description | Operators |
 * |---|---|---|
 * | `connection` | Connection check (explicit) | `exists`, `notExists`, `eq`, `ne` |
 * | `responseContains` | Match response body content | `contains` |
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import groundcover:index/syntheticTest:SyntheticTest example "<id>"
 * ```
 */
export class SyntheticTest extends pulumi.CustomResource {
    /**
     * Get an existing SyntheticTest resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SyntheticTestState, opts?: pulumi.CustomResourceOptions): SyntheticTest {
        return new SyntheticTest(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'groundcover:index/syntheticTest:SyntheticTest';

    /**
     * Returns true if the given object is an instance of SyntheticTest.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SyntheticTest {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SyntheticTest.__pulumiType;
    }

    /**
     * Assertions to validate the check result.
     */
    declare public readonly assertions: pulumi.Output<outputs.SyntheticTestAssertion[] | undefined>;
    /**
     * DNS check configuration. Tests DNS resolution and optionally validates DNSSEC.
     */
    declare public readonly dnsCheck: pulumi.Output<outputs.SyntheticTestDnsCheck | undefined>;
    /**
     * Whether the synthetic test is enabled. Default: `true`.
     */
    declare public readonly enabled: pulumi.Output<boolean>;
    /**
     * HTTP check configuration. Defines the endpoint to monitor.
     */
    declare public readonly httpCheck: pulumi.Output<outputs.SyntheticTestHttpCheck | undefined>;
    /**
     * How often the check runs. Supported values: `15s`, `30s`, `1m`, `5m`, `10m`, `15m`, `30m`, `1h`.
     */
    declare public readonly interval: pulumi.Output<string>;
    /**
     * Extra labels to attach to the synthetic test metrics.
     */
    declare public readonly labels: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * Monitor configuration for the synthetic test. Controls the monitor that is automatically created for this test, including alerting behavior and notification routing.
     */
    declare public readonly monitor: pulumi.Output<outputs.SyntheticTestMonitor | undefined>;
    /**
     * The name of the synthetic test.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Retry policy for failed checks.
     */
    declare public readonly retry: pulumi.Output<outputs.SyntheticTestRetry | undefined>;
    /**
     * SSL/TLS check configuration. Validates SSL certificates and TLS connections.
     */
    declare public readonly sslCheck: pulumi.Output<outputs.SyntheticTestSslCheck | undefined>;
    /**
     * TCP check configuration. Tests TCP connectivity and optionally sends/receives data.
     */
    declare public readonly tcpCheck: pulumi.Output<outputs.SyntheticTestTcpCheck | undefined>;
    /**
     * Configuration schema version. Managed by the provider.
     */
    declare public /*out*/ readonly version: pulumi.Output<number>;

    /**
     * Create a SyntheticTest resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SyntheticTestArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SyntheticTestArgs | SyntheticTestState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SyntheticTestState | undefined;
            resourceInputs["assertions"] = state?.assertions;
            resourceInputs["dnsCheck"] = state?.dnsCheck;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["httpCheck"] = state?.httpCheck;
            resourceInputs["interval"] = state?.interval;
            resourceInputs["labels"] = state?.labels;
            resourceInputs["monitor"] = state?.monitor;
            resourceInputs["name"] = state?.name;
            resourceInputs["retry"] = state?.retry;
            resourceInputs["sslCheck"] = state?.sslCheck;
            resourceInputs["tcpCheck"] = state?.tcpCheck;
            resourceInputs["version"] = state?.version;
        } else {
            const args = argsOrState as SyntheticTestArgs | undefined;
            if (args?.interval === undefined && !opts.urn) {
                throw new Error("Missing required property 'interval'");
            }
            resourceInputs["assertions"] = args?.assertions;
            resourceInputs["dnsCheck"] = args?.dnsCheck;
            resourceInputs["enabled"] = args?.enabled;
            resourceInputs["httpCheck"] = args?.httpCheck;
            resourceInputs["interval"] = args?.interval;
            resourceInputs["labels"] = args?.labels;
            resourceInputs["monitor"] = args?.monitor;
            resourceInputs["name"] = args?.name;
            resourceInputs["retry"] = args?.retry;
            resourceInputs["sslCheck"] = args?.sslCheck;
            resourceInputs["tcpCheck"] = args?.tcpCheck;
            resourceInputs["version"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SyntheticTest.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SyntheticTest resources.
 */
export interface SyntheticTestState {
    /**
     * Assertions to validate the check result.
     */
    assertions?: pulumi.Input<pulumi.Input<inputs.SyntheticTestAssertion>[]>;
    /**
     * DNS check configuration. Tests DNS resolution and optionally validates DNSSEC.
     */
    dnsCheck?: pulumi.Input<inputs.SyntheticTestDnsCheck>;
    /**
     * Whether the synthetic test is enabled. Default: `true`.
     */
    enabled?: pulumi.Input<boolean>;
    /**
     * HTTP check configuration. Defines the endpoint to monitor.
     */
    httpCheck?: pulumi.Input<inputs.SyntheticTestHttpCheck>;
    /**
     * How often the check runs. Supported values: `15s`, `30s`, `1m`, `5m`, `10m`, `15m`, `30m`, `1h`.
     */
    interval?: pulumi.Input<string>;
    /**
     * Extra labels to attach to the synthetic test metrics.
     */
    labels?: pulumi.Input<{[key: string]: pulumi.Input<string>}>;
    /**
     * Monitor configuration for the synthetic test. Controls the monitor that is automatically created for this test, including alerting behavior and notification routing.
     */
    monitor?: pulumi.Input<inputs.SyntheticTestMonitor>;
    /**
     * The name of the synthetic test.
     */
    name?: pulumi.Input<string>;
    /**
     * Retry policy for failed checks.
     */
    retry?: pulumi.Input<inputs.SyntheticTestRetry>;
    /**
     * SSL/TLS check configuration. Validates SSL certificates and TLS connections.
     */
    sslCheck?: pulumi.Input<inputs.SyntheticTestSslCheck>;
    /**
     * TCP check configuration. Tests TCP connectivity and optionally sends/receives data.
     */
    tcpCheck?: pulumi.Input<inputs.SyntheticTestTcpCheck>;
    /**
     * Configuration schema version. Managed by the provider.
     */
    version?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a SyntheticTest resource.
 */
export interface SyntheticTestArgs {
    /**
     * Assertions to validate the check result.
     */
    assertions?: pulumi.Input<pulumi.Input<inputs.SyntheticTestAssertion>[]>;
    /**
     * DNS check configuration. Tests DNS resolution and optionally validates DNSSEC.
     */
    dnsCheck?: pulumi.Input<inputs.SyntheticTestDnsCheck>;
    /**
     * Whether the synthetic test is enabled. Default: `true`.
     */
    enabled?: pulumi.Input<boolean>;
    /**
     * HTTP check configuration. Defines the endpoint to monitor.
     */
    httpCheck?: pulumi.Input<inputs.SyntheticTestHttpCheck>;
    /**
     * How often the check runs. Supported values: `15s`, `30s`, `1m`, `5m`, `10m`, `15m`, `30m`, `1h`.
     */
    interval: pulumi.Input<string>;
    /**
     * Extra labels to attach to the synthetic test metrics.
     */
    labels?: pulumi.Input<{[key: string]: pulumi.Input<string>}>;
    /**
     * Monitor configuration for the synthetic test. Controls the monitor that is automatically created for this test, including alerting behavior and notification routing.
     */
    monitor?: pulumi.Input<inputs.SyntheticTestMonitor>;
    /**
     * The name of the synthetic test.
     */
    name?: pulumi.Input<string>;
    /**
     * Retry policy for failed checks.
     */
    retry?: pulumi.Input<inputs.SyntheticTestRetry>;
    /**
     * SSL/TLS check configuration. Validates SSL certificates and TLS connections.
     */
    sslCheck?: pulumi.Input<inputs.SyntheticTestSslCheck>;
    /**
     * TCP check configuration. Tests TCP connectivity and optionally sends/receives data.
     */
    tcpCheck?: pulumi.Input<inputs.SyntheticTestTcpCheck>;
}
