// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Metrics Aggregation resource. This is a singleton resource that configures metrics aggregation rules.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as groundcover from "@groundcover-com/groundcover";
 *
 * const config = new pulumi.Config();
 * const groundcoverApiKey = config.require("groundcoverApiKey");
 * const groundcoverBackendId = config.require("groundcoverBackendId");
 * // Example Metrics Aggregation
 * // This resource configures metrics aggregation rules for reducing cardinality
 * // and improving query performance.
 * const metricsaggregation = new groundcover.Metricsaggregation("metricsaggregation", {value: `content: |
 *   - ignore_old_samples: true
 *     match: '{__name__=~"http_requests_total"}'
 *     without: [instance, pod]
 *     interval: 60s
 *     outputs: [total_prometheus]
 *   - ignore_old_samples: true
 *     match: '{__name__=~"process_cpu_seconds_total"}'
 *     without: [instance]
 *     interval: 30s
 *     outputs: [total_prometheus]
 * `});
 * export const metricsAggregationUpdatedAt = metricsaggregation.updatedAt;
 * ```
 */
export class Metricsaggregation extends pulumi.CustomResource {
    /**
     * Get an existing Metricsaggregation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: MetricsaggregationState, opts?: pulumi.CustomResourceOptions): Metricsaggregation {
        return new Metricsaggregation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'groundcover:index/metricsaggregation:Metricsaggregation';

    /**
     * Returns true if the given object is an instance of Metricsaggregation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Metricsaggregation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Metricsaggregation.__pulumiType;
    }

    /**
     * The last update timestamp of the metrics aggregation configuration.
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<string>;
    /**
     * The YAML representation of the metrics aggregation configuration.
     */
    declare public readonly value: pulumi.Output<string>;

    /**
     * Create a Metricsaggregation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: MetricsaggregationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: MetricsaggregationArgs | MetricsaggregationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as MetricsaggregationState | undefined;
            resourceInputs["updatedAt"] = state?.updatedAt;
            resourceInputs["value"] = state?.value;
        } else {
            const args = argsOrState as MetricsaggregationArgs | undefined;
            if (args?.value === undefined && !opts.urn) {
                throw new Error("Missing required property 'value'");
            }
            resourceInputs["value"] = args?.value;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Metricsaggregation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Metricsaggregation resources.
 */
export interface MetricsaggregationState {
    /**
     * The last update timestamp of the metrics aggregation configuration.
     */
    updatedAt?: pulumi.Input<string>;
    /**
     * The YAML representation of the metrics aggregation configuration.
     */
    value?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a Metricsaggregation resource.
 */
export interface MetricsaggregationArgs {
    /**
     * The YAML representation of the metrics aggregation configuration.
     */
    value: pulumi.Input<string>;
}
